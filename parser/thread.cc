#include <stdio.h>
#include <stdlib.h>
#include <sys/types.h>

/*
int getThreadID(void)
{
    fprintf(stdout,"IDD --->  %d",pid_t);
    pid_t t = gettid();
    return reinterpret_cast<int>(pid_t);
}
*/
